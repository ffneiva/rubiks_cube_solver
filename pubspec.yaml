name: rubiks_cube_solver
description: "Solver de Cubo Mágico"

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cuber: ^0.4.0
  cupertino_icons: ^1.0.6
  flag: ^7.0.0
  flutter_colorpicker: ^1.0.3
  flutter_cube: ^0.1.1
  flutter_spinkit: ^5.2.1
  image_picker: ^1.0.7
  sensors_plus: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
    - assets/cube/
